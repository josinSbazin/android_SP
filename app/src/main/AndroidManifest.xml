<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.com.rh.sp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".BaseActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            >
        </activity>
        <activity android:name=".AnchorActivity" />
        <activity android:name=".WeightCalc.WeightCalcActivity" />
        <activity android:name=".SortamentActivity" />
        <activity android:name=".ResistanceActivity" />
        <activity android:name=".AreaCalc.AreaCalcActivity"></activity>
    </application>

</manifest>